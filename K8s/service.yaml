apiVersion: v1
kind: Service

metadata:
  name: images-service
  namespace: jenkins

spec:
  selector:
    app: images-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: NodePort
